{
  "name": "parser-js-wrapper",
  "scripts": {
    "build": "chmod +x ../../scripts/source.sh && ../../scripts/build.sh && wasm-pack build --release",
    "test": "wasm-pack test --firefox"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
