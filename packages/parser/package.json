{
  "name": "parser-js-wrapper",
  "scripts": {
    "build": "which cargo && wasm-pack build --release",
    "test": "wasm-pack test --firefox"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
