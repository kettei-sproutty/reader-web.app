{
  "name": "parser-js-wrapper",
  "scripts": {
    "build": "wasm-pack build --release",
    "test": "wasm-pack test --firefox --headless"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
