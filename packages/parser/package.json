{
  "name": "parser-js-wrapper",
  "scripts": {
    "build": "chmod +x ./scripts/build.sh && ./scripts/build.sh",
    "build_2": "wasm-pack build --release",
    "test": "wasm-pack test --firefox"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
