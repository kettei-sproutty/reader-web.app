{
  "name": "parser-js-wrapper",
  "scripts": {
    "build": "if [ $(uname) == 'Linux' ]; then source $CARGO_HOME/env; fi && wasm-pack build --release"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
